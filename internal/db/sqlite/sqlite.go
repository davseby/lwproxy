// package sqlite provides a sqlite database implementation.
package sqlite

// NOTE: Here we could implement a different data saving mechanism,
// rather than using in-memory storage. What is more, we could have wrapping
// databases for retry or caching logic.
